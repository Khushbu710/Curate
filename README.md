# Curated Liquidity Vault

A Uniswap v4 vault that pools user token0/token1 deposits into a single curator-managed liquidity position, with a minimal hook enforcing that only the vault can modify that position and only within the curator's currently approved tick range.

**Status:** repository scaffolding only — Foundry project initialized, dependencies pinned, no vault or hook logic implemented yet.
